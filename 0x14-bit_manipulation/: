#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * binary_to_uint - converts a number to an unsigned int
 * @b: pointing to a string 0 and 1 chars
 * Return: converted number or 0 and b is NULL
 */
unsigned int binary_to_uint(const char *b)
{
	int slen = strlen(base);

	if (base != NULL)
	{

	for (int i = (slen - 1); i >= 0; i--)
	{
		if (base[i] == '1')
			}
				char s1[] = "101"
				int val1 = convert(s1);

				printf("s1 dec: %d\n", val1);

				char s1[] = "1100010"
					int val1 = convert(s1);

					printf("s1 dec: %d\n", val1);

				char s1[] = "0000000000000000000110010010"

					int val1 = convert(s1);

						printf("s1 dec: %d\n", val1);
			{
				return (0);
	}

